---
name: log-sleep
description: 把睡眠狀況（幾點睡、幾點起、睡得如何、有沒有吃助眠藥）記成一晚一筆，經使用者確認後寫入 data/Monkey/sleep/、重建 dashboard 並 commit。使用者可以一次口述講完，也可以只說一句「記睡眠」讓 skill 用選項把缺的欄位問完。當使用者說要記錄睡眠、回報昨晚睡得如何、講吃了或沒吃助眠藥、或要補記某一晚時使用。
---

# 記錄一晚睡眠

把使用者的口語輸入轉成 `data/Monkey/sleep/<date>.json`，重建 dashboard，commit 並 push。

**先確認，再寫檔。** 使用者確認之前不要建立任何檔案、不要 commit。這是唯一的人工關卡，不要跳過。

## 這是誰的紀錄？

**目前只有 Monkey 有睡眠紀錄。** 資料一律寫進 `data/Monkey/sleep/`，頁面是 `dashboard-monkey.html`。

使用者要記 Captain 的睡眠 → **停下來問**。那需要先在 `dashboard-captain.html` 加 SLEEP 標記區塊與集章卡，不是在這裡臨時加一個資料夾了事。

## 這一筆算哪一晚？

**`date` 一律是「就寢那一天」，不是起床那一天。**

早上記錄昨晚的睡眠，`date` 是**昨天**。

`bedtime` 落在凌晨（00:00–05:59）時，實際上床的時鐘日期是 `date` 的**隔天**：
`date: 2026-08-12` + `bedtime: "01:30"` = 8/13 凌晨 1:30 上床、8/13 早上起床，算 8/12 那一晚。

**這是機械規則，不是判斷題。** 算出來直接寫進確認訊息就好，**不要反問使用者「這樣理解對嗎」**
——慣例他已經知道，每天複述一次只是雜訊。只有這兩種情況才停下來問：

- 目標檔案已經存在（見 §補記）
- 使用者給的日期與時間互相矛盾，兩種讀法都說得通

## 流程

1. 讀使用者輸入，依 §解析規則 抽出欄位。
2. 有必填欄位問不出來 → **用 §互動模式 的選項問完**，不要猜、不要填 0、
   不要「先記其他的之後補」。
3. 用繁體中文列出解析結果給使用者確認（格式見下）。有疑慮的地方主動指出。
4. 使用者確認或訂正後，才寫檔 → 重建 → commit → push。

### 確認訊息的格式

```
2026-08-05（三）晚上 · Monkey

💊 有吃
躺床  23:10 – 07:00（7h50）
品質  ★★★☆☆
早上  昏沉
夜醒  1 次

以上正確嗎？確認後我就寫入並更新 dashboard。
```

躺床時數是算給使用者對照用的，**不要寫進 JSON**（推導得出的值不存）。

## 互動模式

使用者常常只丟一句「記睡眠」「記昨晚」，或講到一半就停。**不要回頭要他把細節打完**，
用 `AskUserQuestion` 把缺的欄位問完。

**只問缺的，講過的不要再問一次。** 「昨晚沒吃藥，11 點多睡」→ `date`、`medication.taken`
已經有了，`bedtime` 還要確認分鐘，其餘進選項。全部都講齊了就直接跳確認訊息，一題都不用問。

一次 `AskUserQuestion` 最多 4 題、每題最多 4 個選項，所以缺很多時分兩批：先問**當晚的事實**，
再問**時間與夜醒**。批次順序固定，不要打散。

### 第一批

| 題目 | header | 選項 |
|---|---|---|
| 記哪一晚？ | `哪一晚` | `昨晚` / `前晚` / `今晚` |
| 有吃助眠藥嗎？ | `助眠藥` | `有吃` / `沒吃` |
| 睡得如何？ | `睡眠品質` | `很好`(5) / `不錯`(4) / `普通`(3) / `睡不好`(2) |
| 早上會昏沉嗎？ | `早上` | `昏沉` / `清爽` |

「哪一晚」的選項一律用**算好的日期**寫進 label，例如今天是 8/15 就寫
`昨晚（8/14 五）`——不要只寫「昨晚」讓使用者自己換算。更早的日期走自由輸入。

品質 1（很糟、整晚沒睡）不放選項，走自由輸入。這種晚上很少，佔一格不划算。

### 第二批

| 題目 | header | 選項 |
|---|---|---|
| 幾點睡？ | `就寢` | 見下方「時間選項」 |
| 幾點起？ | `起床` | 見下方「時間選項」 |
| 夜醒幾次？ | `夜醒` | `沒醒過`(0) / `1 次` / `2 次` / `不記得`（省略欄位） |

`3 次以上` 走自由輸入。

**`沒醒過` 寫 `night_wakes: 0`，`不記得` 整個欄位省略。** 這兩個不一樣：一個是「問了，答案是零」，
另一個是「沒有這筆資料」。互動模式下沒問到才省略——這是 §欄位 那條「沒提到就省略，不要填 0」
在有問的情況下的另一半。

### 時間選項

**從歷史紀錄算，不要用寫死的整點。**

讀 `data/Monkey/sleep/` 最近 7 筆，分別統計 `bedtime` 與 `wake_time` 出現次數，
取最常出現的 4 個相異時間當選項。排序：**先看次數，次數相同時最近一次出現比較晚的排前面。**
不要用字串大小排——`09:30` 和 `12:00` 都出現 2 次時，該排前面的是你昨天用的那個，
不是數字小的那個。

排第一的在 description 標「最近最常」。相異時間不足 4 個就補鄰近整點湊滿。

自由輸入一律接受 `HH:MM`、`23:10`、「11 點半」這類寫法，正規化成 24 小時制補兩位數。

### 問完之後

照舊出 §確認訊息的格式 的確認訊息，**選項答完不等於確認過**。使用者按確認才寫檔。

`data/Monkey/sleep/YYYY-MM-DD.json`，檔名就是 `date`：

```json
{
  "date": "2026-08-05",
  "medication": { "taken": true },
  "bedtime": "23:10",
  "wake_time": "07:00",
  "quality": 3,
  "morning_grogginess": true,
  "night_wakes": 1,
  "note": null
}
```

| 欄位 | 必填 | 說明 |
|---|---|---|
| `date` | ✓ | `YYYY-MM-DD`，就寢那一天，**必須與檔名一致** |
| `medication.taken` | ✓ | `true` / `false`，那一晚有沒有吃助眠藥 |
| `bedtime` | ✓ | `HH:MM` 24 小時制，上床時間 |
| `wake_time` | ✓ | `HH:MM`，起床時間 |
| `quality` | ✓ | 1–5 整數，主觀睡眠品質 |
| `morning_grogginess` | ✓ | `true` / `false`，早上起來有沒有昏沉 |
| `night_wakes` | | 夜醒次數，≥ 0 的整數。沒提到就省略，不要填 0 |
| `note` | | 當晚發生了什麼，沒有就寫 `null` |

**六個必填一個都不能少，也不能是 `null`。** `build_dashboard.js` 會 exit 1 擋下來並讓部署失敗
——build 失敗是去修資料，不是繞過檢查。

**沒吃就寫 `{"taken": false}`，不要省略整個 `medication`。** 「那晚沒吃」跟「那晚沒記」是兩件事，
集章卡上長得不一樣。

## 解析規則

### 時間

- 「11 點睡」→ `bedtime: "23:10"` 這類要問清楚分鐘，或直接用整點 `"23:00"`，不要自己編一個 `"23:07"`
- 「大概 11 點多」→ 問一下大概幾分，或用 `"23:30"` 但要在確認訊息裡標明是估的
- 一律 24 小時制、補滿兩位數：`"07:00"` 不是 `"7:00"`

### 品質 1–5

使用者通常用口語。對照表：

| 口語 | `quality` |
|---|---|
| completely 睡不著、整晚翻來覆去、很糟 | 1 |
| 睡不好、淺眠、斷斷續續 | 2 |
| 普通、還好、就那樣 | 3 |
| 不錯、睡得還可以 | 4 |
| 很好、一覺到天亮、超好睡 | 5 |

拿不準就把你的判讀寫進確認訊息讓使用者改，不要自己決定完就寫檔。

### 早上昏沉

「起來有點昏」「頭很重」「爬不起來」→ `true`。
「很清爽」「精神不錯」→ `false`。
**完全沒提到 → 問**，這是必填，不能預設 `false`。

### 有沒有吃藥

使用者說「有吃」「吃了」→ `true`，「沒吃」「今天沒吃」→ `false`。

**沒提到就問。** 使用者只講睡眠沒講吃藥時，回頭問那晚吃了沒——不要從減藥計畫推。
計畫是計畫，吃沒吃是事實，這裡只記事實。

## 補記

漏掉的晚上要能事後補。使用者說「補一下上週三」「8/2 那晚忘了記」時：

1. 算出實際日期，**在確認訊息裡寫出完整日期與星期**——只是寫出來，不是反問，
   算錯的話使用者會直接訂正
2. 其餘流程完全一樣

檔案已經存在 → **停下來問**是要覆蓋還是使用者記錯日期了，不要直接蓋掉。

## 減藥計畫

**資料層沒有減藥計畫，這個 skill 也不管它。**

計畫隨睡眠狀況變動，存了就會跟實際脫節。使用者問「今晚該不該吃」→ 那是他跟醫囑之間的事，
這裡只負責把發生過的事記下來。

## 寫檔之後

```bash
node scripts/build_dashboard.js Monkey
node scripts/test_monkey_metrics.js
```

build 失敗就是資料有問題，回去修資料。測試失敗代表指標邏輯被動到了，不要繞過。

commit message 用繁體中文，格式比照現有紀錄：

```
chore(sleep): 記錄 2026-08-05 的睡眠
```

## 送出：推分支保存，但**不要自己 merge**

跟 `.claude/skills/log-workout/` 同一套規則，兩邊要一致。部署只發生在 merge 進 `master` 的那一刻，
所以這兩件事要分開看：

1. **開分支 → commit → push 分支** —— 做完就做，不用問。只在本地 commit 救不回來。
2. **開 PR、merge** —— **一定要等使用者說**。這一步等於部署，時機是使用者的決定，不是你的。

push 完回報寫了什麼、build 與測試結果，然後停下來。

使用者說要 merge 之後：

1. `gh pr create --base master --head <自己的分支> --title <標題> --body-file -` 開 PR。
   **一律用 `gh`，不要用 `mcp__github__*` 的寫入類工具**：MCP 走的 GitHub App 只有讀權限，
   開 PR 會回 403 Resource not accessible by integration。讀取類的 MCP 工具照樣可用。
2. 等 PR 上的 `validate` 綠燈，**沒過就回去修資料，不要繞過**。
3. `gh pr merge <PR#> --squash --delete-branch` merge。
   **`--delete-branch` 不能省**：GitHub repo 設定的 Automatically delete head branches
   只刪得掉 remote 分支，本地那支要靠這個 flag 才會一起清掉，否則會一直累積。
4. merge 成功後告訴使用者 GitHub Actions 正在部署，約 30-60 秒後
   https://captain-tim.github.io/project_protein/ 就是最新的。
